

export const fixacao = (palavra: string): string[] => {
    // const result = []

    // for(let i = 0; i < palavra.length; i++){
    //     result.push(palavra[i])
    // }
    // return result

    return palavra.split("")
}